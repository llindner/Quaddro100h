<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          
    package="br.com.luizlindner.quaddro100h">

    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".lab01.app.controller.SplashActivity" android:noHistory="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".lab01.app.controller.MainActivity" />
        <activity android:name=".lab02.app.controller.MegasenaActivity"></activity>
        <activity android:name=".lab03.app.controller.TictactoeActivity"></activity>
        <activity android:name=".lab04.app.controller.EnderecoActivity"></activity>

    </application>

</manifest>
